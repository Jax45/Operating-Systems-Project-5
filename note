add need matrix to pcb
add wait to user request for specific pid
change oss to not send request back until later and only after (sim algorithm here)


if that is done then we can sub in the algorithmn

